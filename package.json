{
  "name": "veolve-coming-soon",
  "version": "1.0.0",
  "description": "Veolve coming soon landing page",
  "scripts": {
    "dev": "npx http-server src -p 5173 --cors -o",
    "build": "echo 'Static site — no build step required'",
    "start": "npx http-server src -p 8080",
    "test": "bash tests/smoke_test.sh"
  },
  "dependencies": {
    "docker": "^1.0.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
